// const { Vendor, Category, Review, Pending, User } = require("../../models");
// const router = require("express").Router();

// const dashboard = document.querySelector('#dashboard')
// console.log(loggedIn)
// console.log(req.session.user_id)

// if(req.session.user_id){
//     dashboard.setAttribute('href', `/user-dashboard/${req.session.user_id}`)
// }
// else if(req.session.vendor_id){
//     dashboard.setAttribute('href', `/vendor-dashboard/${req.session.vendor_id}`)
// }

